// Função para finalizar a votação e redirecionar para a página de confirmação
function finalizarVotacao() {
    // Exemplo de mensagem de confirmação (você pode personalizar a mensagem conforme necessário)
    alert("Votação confirmada com sucesso!");

    // Redireciona para a página de confirmação
    window.location.href = "confirmacao.html"; // Aqui a página confirmacao.html será carregada
}